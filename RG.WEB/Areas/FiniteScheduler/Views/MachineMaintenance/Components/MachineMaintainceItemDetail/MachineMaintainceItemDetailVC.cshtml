@model List<RG.Application.ViewModel.FiniteScheduler.Business.MachineMaintenance.MonthlyMachineMaintainceItemsVCVM>

<div class="card">
    @*<div class="card-header m-1 p-1"><h5 class="m-0 b">@machineGroups.MachineGroup</h5></div>*@
    <div class="card-body p-1" style="overflow-x:scroll">
        @foreach (var machineGroups in Model)
        {


            <table class="table table-sm table-striped table-bordered text-center text-xs">                
                <tbody>
                    @foreach (var itemLocation in machineGroups.GroupWiseMonthlyMachineMaintainceItems)
                    {
                        var rowSL = 1;
                      

                        @foreach (var itemDates in itemLocation.MonthlyMachineMaintainceCheckDates)
                        {

                    <tr>
                        @if (rowSL == 1)
                        {
                            <td>@itemLocation.LocationName</td>
                            <td>@itemLocation.MachineName</td>
                        }
                        else
                        {
                            <td></td>
                            <td></td>
                        }
                        <td>@itemDates.CheckDateMsg</td>
                        <td>@itemDates.ScheduleDateMsg</td>

                        @foreach (var items in machineGroups.MachineCheckItems)
                        {
                            var itemInCheckList = itemDates.CheckDateWiseItems.Where(x => x.SerialNo == items.SerialNo).FirstOrDefault();
                            if (itemInCheckList == null)
                            {
                                <td></td>
                            }
                            else
                            {
                                @*<td class="popoverData" onmouseover="asd()">@itemInCheckList.ItemName</td>*@

                                <td>
                                   
                                    <a class="btn popoverData" href="#" data-content="Popover with data-trigger" rel="popover" data-placement="bottom" data-original-title="Title" data-trigger="hover" onmouseover="asd()">
                                        @itemInCheckList.ItemName
                                    </a>
                                   </td>

                <!--colCount++;
                if (itemInCheckList.MechanicalChecked)
                {
                    <td>
                        @itemInCheckList.ItemName-->
                                    @*<a class="btn popoverData" href="#" data-content="Popover with data-trigger" rel="popover" data-placement="bottom" data-original-title="Title" data-trigger="hover">
                        <i class="fa fa-check text-success" aria-hidden="true"></i>
                    </a>*@
                                    <!--</td>
                }
                else
                {
                    <td>
                        <i class="fa fa-times text-danger" aria-hidden="true"></i>
                    </td>
                }
                if (itemInCheckList.ElectricalChecked)
                {
                    <td><i class="fa fa-check text-success" aria-hidden="true"></i></td>
                }
                else
                {
                    <td><i class="fa fa-times text-danger" aria-hidden="true"></i></td>
                }-->
                                    }

                                    }
                            </tr>
                            rowSL++;
                        }
                        
                    }
                </tbody>
            </table>


        }
    </div>
</div>






